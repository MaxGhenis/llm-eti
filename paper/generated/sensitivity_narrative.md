The model-specific clean-pair samples produce taxable-income slopes of **0.82** for Claude, **0.42** for DeepSeek, **0.26** for Gemma, and **0.37** for GPT-4o mini. Year-stratified estimates are reported because the source composition and DeepSeek coverage differ sharply by year; they are exploratory selection checks rather than prespecified subgroup tests. On the primary positive-output panel, DeepSeek's tax-cut-only slope is **0.175** and its tax-increase-only slope is **0.539**; a fully interacted direction test gives **p = 0.0034**. The equal-year-weight rows give each source year the same total regression weight. The proportional-change rows are scale-free and retain zero outputs. The log(1 + dollar income) rows also retain zeros but depend on the dollar unit and are not interpreted as elasticities.
